<%= form_for([@recipe, @comment]) do |f| %>

<div class="field">
    <%= f.label :text %>
    <%= f.text_field :text%>
    </div>
    
  <div class="actions">
      <%= f.submit %>
  </div>

<% end %>