<%= form_for(@ingredient) do |f| %>

<div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.hidden_field :recipe_id %>
    </div>
    
  <div class="actions">
      <%= f.submit %>
  </div>

<% end %>